{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<main class="container my-5">
    <h1 class="h3 mb-4">Enregistrer un nouvel utilisateur</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
        {{ form_row(registrationForm.roles) }}  

        <button type="submit" class="btn btn-success mt-2">Valider l'inscription</button>
        <a href="{{ path('app_admin_user_index') }}"><button type="button" class="btn btn-secondary mt-2">Retour à la liste</button></a>

    {{ form_end(registrationForm) }}
</main>
{% endblock %}
