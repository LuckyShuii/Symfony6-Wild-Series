{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">{{ button_label|default('⊕ Ajouter') }}</button>

    <a href="{{ path('app_admin_user_index') }}"><button type="button" class="btn btn-secondary mt-2">Retour à la liste</button></a>
{{ form_end(form) }}
